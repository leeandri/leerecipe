{% extends 'base.html.twig' %}

{% block title %}LeeRecipe - Edit user password{% endblock %}

{% block body %}

<div class="container mt-4">
    <h1 class="mt-4">User's password edit form</h1>
    
    {% for message in app.flashes('warning') %}
        <div class="alert alert-success mt-4">
            {{ message }}
        </div>
    {% endfor %}
    
    {{form(form)}}
</div>

{% endblock %}
